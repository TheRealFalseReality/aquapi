substitutions:
  update_firmware_sensor_interval: 6h

http_request:
  verify_ssl: false

update:
  - platform: http_request
    name: Firmware Update
    update_interval: "${update_firmware_sensor_interval}"
    id: update_firmware_sensor
    entity_category: 'diagnostic'
    icon: mdi:update
    source: https://raw.githubusercontent.com/TheRealFalseReality/aquapi/gh-pages/manifest.json

ota:
  - platform: http_request

button:
  - platform: template
    id: update_firmware
    name: Update Firmware (Experimental)
    entity_category: "config"
    icon: mdi:update
    # disabled_by_default: true
    on_press: 
      then:
        - ota.http_request.flash:
            md5_url: https://raw.githubusercontent.com/TheRealFalseReality/aquapi/gh-pages/aquapi-esp32/aquapi.md5
            url: https://raw.githubusercontent.com/TheRealFalseReality/aquapi/gh-pages/aquapi-esp32/aquapi-esp32.ota.bin